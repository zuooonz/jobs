name: ai_core
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - fastapi
      - uvicorn
      - psycopg2-binary
      - python-dotenv
      - openai
      - pydantic
